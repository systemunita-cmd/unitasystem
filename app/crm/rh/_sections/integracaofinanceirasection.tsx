"use client";import{useEffect,useMemo,useState}from"react";import{supabase}from"../../../lib/supabase";const dinheiro=(v:number)=>Number(v||0).toLocaleString("pt-BR",{style:"currency",currency:"BRL"}),atual=()=>new Date().toISOString().slice(0,7);const campo={border:"1px solid #cbd5e1",borderRadius:9,padding:"8px 10px",minHeight:40}as const,card={background:"#fff",border:"1px solid #e2e8f0",borderRadius:13,padding:14}as const;
export function IntegracaoFinanceiraSection(){const[comp,setComp]=useState(atual()),[itens,setItens]=useState<any[]>([]),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false),[detalhe,setDetalhe]=useState<any>(null),[funcionarios,setFuncionarios]=useState<any[]>([]),[beneficios,setBeneficios]=useState<any[]>([]),[vinculos,setVinculos]=useState<any[]>([]),[funcionarioId,setFuncionarioId]=useState(""),[beneficioId,setBeneficioId]=useState(""),[valor,setValor]=useState(0);const carregar=async()=>{const[f,fn,b,v]=await Promise.all([supabase.from("folha_itens").select("*").eq("competencia",comp).order("nome"),supabase.from("funcionarios").select("id,nome,cargo,status").neq("status","desligado").order("nome"),supabase.from("beneficios").select("id,nome,tipo,custo_empresa").order("nome"),supabase.from("rh_beneficio_funcionarios").select("*").eq("ativo",true)]);setItens(f.data||[]);setFuncionarios(fn.data||[]);setBeneficios(b.data||[]);setVinculos(v.data||[])};useEffect(()=>{carregar()},[comp]);const sincronizar=async()=>{setBusy(true);const{data,error}=await supabase.rpc("sincronizar_financeiro_rh",{p_competencia:comp});setMsg(error?error.message:`Sincronizado: ${JSON.stringify(data)}`);setBusy(false);carregar()};const vincular=async()=>{if(!funcionarioId||!beneficioId)return;const custo=valor||Number(beneficios.find(x=>x.id===beneficioId)?.custo_empresa||0);const{error}=await supabase.from("rh_beneficio_funcionarios").upsert({funcionario_id:funcionarioId,beneficio_id:beneficioId,valor_empresa:custo,ativo:true},{onConflict:"funcionario_id,beneficio_id"});setMsg(error?error.message:"Benefício vinculado.");carregar()};const remover=async(id:string)=>{await supabase.from("rh_beneficio_funcionarios").update({ativo:false}).eq("id",id);carregar()};const t=itens.reduce((a,x)=>({sal:a.sal+Number(x.base||0),vt:a.vt+Number(x.vale_transporte||0),va:a.va+Number(x.vale_alimentacao||0),ben:a.ben+Number(x.beneficios||0),enc:a.enc+Number(x.encargos_empresa||0),com:a.com+Number(x.comissao||0),bonus:a.bonus+Number(x.bonus_meta||0)}),{sal:0,vt:0,va:0,ben:0,enc:0,com:0,bonus:0}),nomes=useMemo(()=>({f:Object.fromEntries(funcionarios.map(x=>[x.id,x.nome])),b:Object.fromEntries(beneficios.map(x=>[x.id,x.nome]))}),[funcionarios,beneficios]);return <div style={{display:"grid",gap:16}}><div><h1 style={{margin:0}}>RH → Financeiro</h1><p style={{fontSize:12,color:"#64748b"}}>Clique em um funcionário para auditar a origem de todos os valores.</p></div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><input type="month" value={comp} onChange={e=>setComp(e.target.value)} style={campo}/><button onClick={sincronizar} disabled={busy}>Sincronizar competência</button>{msg&&<span>{msg}</span>}</div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(120px,1fr))",gap:8}}>{[["Salários",t.sal],["VT",t.vt],["Alimentação",t.va],["Benefícios",t.ben],["Encargos",t.enc],["Comissões",t.com],["Bônus",t.bonus]].map(([l,v])=><div key={String(l)} style={card}><small>{l}</small><b style={{display:"block"}}>{dinheiro(Number(v))}</b></div>)}</div><div style={card}><h3>Benefícios por funcionário</h3><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><select value={funcionarioId} onChange={e=>setFuncionarioId(e.target.value)} style={campo}><option value="">Funcionário</option>{funcionarios.map(x=><option key={x.id} value={x.id}>{x.nome}</option>)}</select><select value={beneficioId} onChange={e=>{setBeneficioId(e.target.value);setValor(Number(beneficios.find(x=>x.id===e.target.value)?.custo_empresa||0))}} style={campo}><option value="">Benefício</option>{beneficios.map(x=><option key={x.id} value={x.id}>{x.nome}</option>)}</select><input type="number" value={valor} onChange={e=>setValor(Number(e.target.value))} style={campo}/><button onClick={vincular}>Vincular</button></div>{vinculos.map(x=><div key={x.id} style={{display:"grid",gridTemplateColumns:"1fr 1fr 120px 80px",padding:7,borderBottom:"1px solid #eee",fontSize:11}}><span>{nomes.f[x.funcionario_id]}</span><span>{nomes.b[x.beneficio_id]}</span><b>{dinheiro(x.valor_empresa)}</b><button onClick={()=>remover(x.id)}>Remover</button></div>)}</div><div style={{...card,overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse",fontSize:11}}><thead><tr>{["Funcionário","Salário","VT","VA","Benefícios","Encargos","Comissão","Bônus","Meta"].map(h=><th key={h} style={{padding:9,textAlign:"left"}}>{h}</th>)}</tr></thead><tbody>{itens.map(x=><tr key={x.id} onClick={()=>setDetalhe(x)} style={{borderTop:"1px solid #eee",cursor:"pointer"}}><td style={{padding:9}}><b>{x.nome}</b><br/>{x.cargo}</td><td>{dinheiro(x.base)}</td><td>{dinheiro(x.vale_transporte)}</td><td>{dinheiro(x.vale_alimentacao)}</td><td>{dinheiro(x.beneficios)}</td><td>{dinheiro(x.encargos_empresa)}</td><td>{dinheiro(x.comissao)}</td><td>{dinheiro(x.bonus_meta)}</td><td>{x.comissao_detalhes?.quantidade||0}/20</td></tr>)}</tbody></table></div>{detalhe&&<div onClick={()=>setDetalhe(null)} style={{position:"fixed",inset:0,background:"rgba(15,23,42,.52)",zIndex:3000,display:"grid",placeItems:"center",padding:20}}><div onClick={e=>e.stopPropagation()} style={{background:"#fff",borderRadius:18,width:"min(760px,100%)",maxHeight:"88vh",overflow:"auto",padding:22}}><div style={{display:"flex",justifyContent:"space-between"}}><div><h2 style={{margin:0}}>Auditoria da folha</h2><p>{detalhe.nome} · {comp}</p></div><button onClick={()=>setDetalhe(null)}>Fechar</button></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:9}}>{[["Salário fixo · RH",detalhe.base],["Vale-transporte · RH",detalhe.vale_transporte],["Vale-alimentação · RH",detalhe.vale_alimentacao],["Benefícios · RH",detalhe.beneficios],["Encargos · RH",detalhe.encargos_empresa],["Comissão · CRM",detalhe.comissao],["Bônus de meta · CRM",detalhe.bonus_meta]].map(([l,v])=><div key={String(l)} style={card}><small>{l}</small><b style={{display:"block"}}>{dinheiro(Number(v))}</b></div>)}</div><Detalhes titulo="Comissão" dados={detalhe.comissao_detalhes}/><Detalhes titulo="Bônus" dados={detalhe.bonus_detalhes}/></div></div>}</div>}
function Detalhes({titulo,dados}:any){return <details style={{marginTop:12,background:"#f8fafc",borderRadius:12,padding:12}}><summary style={{fontWeight:800,cursor:"pointer"}}>Detalhes de {titulo}</summary><pre style={{fontSize:10,whiteSpace:"pre-wrap"}}>{JSON.stringify(dados||{},null,2)}</pre></details>}